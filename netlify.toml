# Netlify build configuration for Press.
# Netlify reads this automatically on import — no manual settings needed.

[build]
  command = "npm run build"
  publish = "dist"

# Single-page app: serve index.html for any path so the app always loads.
[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
